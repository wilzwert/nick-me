import { createTheme } from "@mantine/core";

// theme.ts
export const theme = createTheme({
  primaryColor: 'violet',
  fontFamily: 'Inter, sans-serif',
  radius: {
    sm: '4px',
    md: '8px',
  },
});
